# OCR lengkap (PaddleOCR + preprocessing). Pasang setelah inti:
#   pip install -r requirements-ocr.txt
#
# PENTING — Python 3.14 (dan versi sangat baru lainnya):
#   paddlepaddle belum merilis wheel di PyPI untuk interpreter tersebut, sehingga
#   pip memunculkan: "No matching distribution found for paddlepaddle".
#   Solusi:
#   - Hanya perlu jalur Gemma Vision + preprocessing ringan:
#       pip install -r requirements-ocr-opencv.txt
#   - Butuh PaddleOCR penuh: buat venv dengan Python 3.10 / 3.11 / 3.12, lalu
#       pip install -r requirements-ocr.txt
#
-r requirements.txt

paddlepaddle>=3.0.0
paddleocr>=2.8.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
